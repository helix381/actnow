# 画布 PRD · 模块计划（Make-PRD）

> **范围**：制作期「无限画布工作台」专项 PRD，独立成册。
> **与主 PRD 关系**：本册**细化并取代** `prd/PRD.md` 模块 6f（PC 无限画布）；资产/分镜/生成等业务语义仍以主 PRD 6b/6c/8 为准，本册只补画布上的**结构、交互、呈现**，不重复业务规则与生成状态机全文（引用即可）。
> **输入源**：本对话讨论范式（PD-6/PD-10）、鼠标/触控范式修正、单分镜=组件粒度修正；主 PRD 6f / _prototype-deltas.md。
> **交付物**：`prd/Canvas/PRD-Canvas.md`，追加日志 `prd/Canvas/_append-log.md`。
> 起始：2026-06-11

## 模块清单（画布专项定制）

| # | 模块 | 内容要点 | 复杂度 | 核心待确认 |
|---|------|---------|--------|-----------|
| C0 | 文档头·背景·与主 PRD 关系 | 画布为何独立成册；本册取代 6f 的边界；术语表（Frame/组件/简单卡片/端口/连线/组合技）| 低 | — |
| C1 | 设计目标与范式参照 | 对标 n8n / Dify / LibTV 导演台 / Dreamina；设计原则（节点扁平·步骤分区·素材自由）| 低 | — |
| C2 | 坐标系与视图模型 | 世界/屏幕坐标换算、缩放范围与光标锚点、自适应 fit、整理布局、小地图 | 中 | — |
| C3 | **交互范式（鼠标+触控+iOS）** ★ | 左键框选 / 空格·中键平移 / 双击空白=添加菜单 / 滚轮·双指缩放 / 触控板 / iOS 单指·双指·长按映射 | 高 | iOS 单指拖=平移还是框选 |
| C4 | Frame 步骤画板 | 五 Frame、resize（边/角 handle）、步骤色淡底、拖头带走框内节点、空间包含判定、overflow 连线穿出 | 中 | — |
| C5 | **卡片体系（一）：组件** ★ | 两层模型；组件清单与**粒度**（单分镜=组件、平行链 ②③④按单镜拆）；组件 anatomy；角色/场景/道具/分镜/分镜图/视频/合成台各组件字段与操作区 | 高 | **③④是否也按单镜拆** |
| C6 | 卡片体系（二）：简单卡片 | 文本/图片/视频/音频四类；拖入·粘贴自动建卡（落点即位置）；anatomy；单输出端口 | 中 | — |
| C7 | 端口与连线 | 左入右出端口；简单卡片→组件=喂素材；组件→组件=系统自动顺序连线 + 用户手动改；坐标换算；贝塞尔样式；非法连线拦截 | 中 | — |
| C8 | 工具层（功能层级） | 图层面板 / 参考线（ruler 拖出）/ 吸附（网格+节点边缘）/ 对齐分布 / 小地图 / 右下角控件区布局 | 中 | 一期/二期切分 |
| C9 | 组合技按钮体系 | 组件右下角一键多步（自动建下游节点+连线+预填+触发生成）；预置清单 + 用户自定义；与 Skill 概念打通 | 中 | — |
| C10 | 状态联动·埋点·待决 | 节点生成状态在画布的呈现（引用主 PRD 模块8）；画布操作埋点；待决问题清单 + 自检 | 低 | — |

## 复杂度裁剪说明

- **不重复**主 PRD 已有内容：生成任务状态机（模块8）、人在环交付格式（模块9）、Agent 编排（模块10）、数据模型（模块7）——本册仅在 C10 引用并补画布呈现层。
- **重点厚做** C3 交互范式、C5 组件体系——这两块是本册存在的理由，也是原型当前最大缺口。

## 待你拍板（确认后逐模块开写）

1. **模块清单**是否认可（增/删/并）？
2. **组件粒度**：②③④都按单镜拆成独立组件、形成平行链（我的推荐）——确认？
3. 交付文件名 `PRD-Canvas.md`，放本目录——确认？

> 确认后按 skill 流程：每次只写 1 个模块，追加到 `PRD-Canvas.md` 末尾，写完停下等你审，再继续下一模块。
