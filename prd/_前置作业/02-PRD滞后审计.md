# 主 PRD 滞后审计表

> **用途**：5 路子 agent 并行通读 shaping / prd 增量 / prototype / tech / specs 全部相关文件后，汇总「主 PRD v0.30 相对各层的滞后、冲突、断层」，作为后续**逐模块重构 PRD** 的施工图。
> **生成**：2026-06-16 · 数据来源：5 路并行通读（shaping / prd增量 / prototype / tech / specs）
> **状态图例**：🔴 冲突(三方给了矛盾答案,需拍板) ／ 🟠 断层(某层有、他层完全没接) ／ 🟡 滞后(PRD 缺、需补入) ／ 🟢 已对齐
> **原则**：不删旧想法；本表只记差异与去向，**不改 PRD 正文**；逐条注明来源层与应入模块。

---

## A. 三方打架的决策（🔴 最高优先级 · 重写 PRD 前必须先拍板）

| # | 议题 | 主 PRD v0.30 | 后端 tech/specs | 原型 | 出处 |
|---|------|--------------|-----------------|------|------|
| A1 | 无限画布引擎 | **tldraw**（6f-R6 / 4.5） | **React Flow**（已确认反转 TQ3/SG-002；s2/s3/s6/s7 全用） | 自研 canvas（workspace.html） | tech/00·01，specs/s2 |
| A2 | Agent 编排框架 | **LangGraph.js** 或自研（4.5 / 模块10） | **自研 Harness 为主**，LangGraph 降为"可替换执行器"；s7 明确排除 LangGraph | — | tech/00·02，specs/s7 |
| A3 | Asset 数据建模 | "**不是**独立实体表，是资产库聚合视图"（7.1 注） | **独立实体表** `assets(id,owner_scope,type,uri,metadata,source)` | — | tech/02·04，specs/s3 |
| A4 | 资产跨项目复用 | 列入 **MVP 做**（模块3 / 6b-R6） | 推到**以后**，MVP 只做项目内（s3 Out of Scope） | — | specs/s3 |

> 这 4 条不是"PRD 落后"，是三份文档对同一件事给了**互相矛盾**的答案。PRD 要立准星，先定哪个为准。

---

## B. 层间断层（🟠 某层画了饼、他层完全没接）

| # | 能力 | 谁定义了 | 谁没接 | 说明 |
|---|------|----------|--------|------|
| B1 | **灵感 Agent 三层出参 / 天眼层 Canonical IR** | 原型 PD-11（正面层/天眼层/剧本层契约） | tech 9 份 + specs 8 切片 **0 命中**；PRD 无 | 原型郑重定义、后端完全没开工。这是核心断层，去向需单独决定 |
| B2 | Skill 可配置体系（4 类技能 + 模型选择 + 可编辑提示词模板 + 拆镜规则） | 原型 workspace.html | 后端无对应规格 | PRD 仅"挂载 Skill 按钮存在"，无内容 |
| B3 | 工作流/场景模板对象（按 N 节点计、存为模板/一键使用） | 原型 + PRD 6f-R4/R8 提概念 | specs 8 切片**均未建表** | 模板作为数据对象无人落地 |

---

## C. 待拍板决策清单（汇总 · 定了才逐模块动笔）

| 编号 | 决策 | 选项 |
|------|------|------|
| D1 | 无限画布引擎（A1） | tldraw ／ React Flow ／ 自研 — 三方统一为哪个 |
| D2 | Agent 编排框架（A2） | LangGraph.js ／ 自研 Harness |
| D3 | Asset 数据建模（A3） | 聚合视图 ／ 独立实体表 |
| D4 | 资产跨项目复用（A4） | MVP 就做 ／ 推到 Roadmap |
| D5 | **天眼层 Canonical IR（B1）** | 保留并要求后端接 ／ 降级为 Roadmap 设想 ／ 其他定位 |
| D6 | 创作设置删「时长」（PD-4） | 确认删 ／ 保留（PD-4 自注：属定稿后收窄，需你确认） |
| D7 | 模块 6f 与 Canvas 专项册关系 | 6f 改为引用 `Canvas/PRD-Canvas.md` ／ 把 Canvas 册并回 6f |

---

## D. 元问题 / 文件健康（整理时要顺手处理）

- **版本基线偏差**：`_prototype-deltas.md` 抬头与行号引用都是相对 **PRD v0.20**，主 PRD 现为 **v0.30**（1511 行）。并入时行号需按 v0.30 重新定位，不能直接套旧行号。
- **PRD 版本号自相矛盾**：开头修改记录到 v0.30，但末尾 [PRD.md:1510] 仍写"当前为 v0.16 草稿"。
- **shaping 失效引用**：shaping 01/04 两处指向 `07-agent-team.md`，该文件不存在（内容已并入 PRD 模块10）。
- **specs 交叉引用悬空**：specs 多处引用 `TQ10` 等编号（指向 tech 的待确认项），PRD 无 TQ 体系、未交叉引用。
- **损坏文件**：`tech/_planning-log.md`（仅前 ~11/45 行有效，其余上万行"（）"占位垃圾）；根目录 `CLAUDE.md`（全文无换行 + 伪标签，需修复格式）。
- **shaping 整体滞后于 PRD**（非 PRD 缺陷）：入口统一 / 版权免责 / 步骤重排 / 组合技 这 4 项 PRD 后续改动，shaping 原文未回写。如要保持一致需回头更新 shaping，但**不阻塞 PRD 整理**。
