# S2 Quickstart

1. Complete S1 and get `project_id`.
2. POST `/api/projects/{projectId}/script/lock`.
3. GET `/api/projects/{projectId}/workspace`.
4. Confirm scenes, shots, and canvas document exist.
