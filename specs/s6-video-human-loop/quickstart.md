# S6 Quickstart

1. Complete S4, optionally S5.
2. POST `/api/generation-tasks` with `gen_type=video`, `backend_mode=human_loop`.
3. Export prompt package.
4. Upload one video file.
5. Confirm task and video node become `completed`.
