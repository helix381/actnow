# S1 Quickstart

1. Start Docker Compose services.
2. POST `/api/projects` with `{ "initial_input": "一个赛博小镇短剧" }`.
3. Open workspace with returned project id.
4. Confirm chat stage renders and thread events are readable.
